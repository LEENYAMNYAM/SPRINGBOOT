#spring.application.name=board01

    # ???? setting
    server.port=8083

    # database setting
    spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
    spring.datasource.url=jdbc:mysql://localhost:3306/sbdb
    spring.datasource.username=root
    spring.datasource.password=1234

    # dto location(mapper?? resultType? ?? ??? ?? ? ????)
    mybatis.type-aliases-package=org.jmt.board01.dto

    # log setting(LOG4j2? ??? ???? ???? ?)
    logging.level.org.springframework = info
    logging.level.org.jmt = debug

    # view setting
    spring.mvc.view.prefix=/WEB-INF/views/
    spring.mvc.view.suffix=.jsp

    # xml mapper location (**? ????? 0? ?? ?? ? ??? ??.)
        # resources/mappers ??? ?? main/java/org.jmt.board01/mapper??? Mapper? ?? ??? ??? ???)
    mybatis.mapper-locations=classpath:mappers/**/*.xml
